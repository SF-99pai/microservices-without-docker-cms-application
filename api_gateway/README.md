# API Gateway

A minimal FastAPI gateway scaffold for routing requests to the student, teacher, and department services.

## Run locally

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
```
